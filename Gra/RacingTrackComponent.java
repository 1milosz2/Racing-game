import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

public class RacingTrackComponent extends Actor {
    public RacingTrackComponent(String image) {
        setImage(image);
    }
}
